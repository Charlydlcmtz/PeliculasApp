/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "./src/**/*.{html,ts}" // Indica que Tailwind escaneará archivos HTML y TypeScript en `src`
    ],
    theme: {
        extend: {},
    },
    plugins: [],
    };
  